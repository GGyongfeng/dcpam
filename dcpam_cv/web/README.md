# DCPAM Web

React + Three.js viewer for DCPAM measurement CSV and `calibration.toml`.

```bash
npm install
npm run dev
```

From the repository root, the same dev server can be started with:

```bash
uv run dcpam ui
```
