# 开发规范

## 文档先行

1. **新功能**：先在 `docs/design.md` 中补充设计，讨论确认后再开发
2. **修改功能**：先更新 `docs/design.md` 对应部分，再改代码
3. **文档即真相**：代码实现必须与设计文档一致，发现不一致时以文档为准并同步修改

## TODO 管理

在 `docs/TODO.md` 中维护待办事项。

规则：
- 完成一项，立即删除，不保留已完成记录
- TODO 只记录待做的事，不做归档

## 提交规范

- 每完成一项 TODO，进行一次 git commit
- 用户提出的任何要求，先写到对应文档中，再执行开发
